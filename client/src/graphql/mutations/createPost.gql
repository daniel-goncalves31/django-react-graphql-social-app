mutation CreatePost($postInput: PostInputType!) {
  createPost(postInput: $postInput) {
    post {
      id
      text
      image
      createdAt
      user {
        id
        name
      }
    }
  }
}
