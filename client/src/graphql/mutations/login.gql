mutation Login($username: String!, $password: String!) {
  tokenAuth(username: $username, password: $password) {
    user {
      id
      username
      email
      firstName
      lastName
      isSuperuser
      lastLogin
      username
      photo
      isStaff
      isActive
      dateJoined
      backImage
    }
  }
}
